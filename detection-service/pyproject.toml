[project]
name = "buildscan-detection"
version = "1.0.0"
description = "BuildScan AI detection service with YOLOv8, SAHI, and Depth Anything V2"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]